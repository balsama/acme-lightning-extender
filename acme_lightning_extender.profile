<?php
/**
 * @file
 * This profile runs as a child profile with a dependency on Lightning.
 * Currently, Drupal does not support installing subprofiles, only modules and
 * themes contained in them. Place all code normally run here inside a custom
 * module within this profile.
 */
